export enum ReqBodyParts {
  BODY = "body",
  PARAMS = "params",
  QUERY = "query",
}

export enum Difficulty {
  EASY = "Easy",
  NORMAL = "Normal",
  HARD = "Hard",
}

export enum Status {
  INCOMPLETE = "Incomplete",
  COMPLETE = "Complete",
}

export enum CardType {
  TASK = "Task",
  CHALLENGE = "Challenge",
}
